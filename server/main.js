import { Meteor } from 'meteor/meteor';

// definir bdd

Meteor.startup(() => {
    // code to run on server at startup
});

Meteor.methods({
    // methodes
    'maMethode'(params) {

    }
});

// publish
